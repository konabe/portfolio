---

---

<div class="header">
  <div class="main-title">
    <a href="/">nabe's gallery</a>
  </div>
  <div class="links">
    <a href="/photograph">photograph</a>
    <a href="/blog">blog</a>
    <a href="/app">app</a>
    <a href="/bio">bio</a>
  </div>
</div>

<style lang="scss">
  .header {
    display: flex;
    flex-direction: row;
    column-gap: 64px;

    .main-title {
      font-size: 40px;

      a {
        text-decoration: none;
        color: var(--color-text);
        bottom: -1px;

        position: relative;

        // アンダーライン
        &::after {
          position: absolute;
          left: 0;
          content: "";
          width: 100%;
          height: 1px;
          background: white;
          bottom: -8px;
          transform: scale(0, 1);
          transform-origin: right top;
          transition: transform 0.2s;
        }

        &:hover::after {
          transform: scale(1, 1);
          transform-origin: left top;
        }
      }
    }

    .links {
      display: flex;
      flex-direction: row;
      column-gap: 32px;
      align-items: flex-end;

      a {
        color: var(--color-text);
        bottom: -1px;
        font-size: 24px;
        text-decoration: none;
        position: relative;

        // アンダーライン
        &::after {
          position: absolute;
          left: 0;
          content: "";
          width: 100%;
          height: 1px;
          background: white;
          bottom: -8px;
          transform: scale(0, 1);
          transform-origin: right top;
          transition: transform 0.2s;
        }

        &:hover::after {
          transform: scale(1, 1);
          transform-origin: left top;
        }
      }
    }
  }
</style>
